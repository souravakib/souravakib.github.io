---
interface Props {
  title: string;
}

const { title } = Astro.props;
---

<section class="mb-8">
  <h2 class="section-title">{title}</h2>
  <div class="mt-4 h-1 w-12 rounded-full bg-accent" />
</section>
